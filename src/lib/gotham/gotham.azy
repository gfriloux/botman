Azy_Namespace Gotham;

struct Command_Json_Answer
{
   string        command;
   string        parameters;
   string        status;

   array<string> content;
};

struct Conf_Xmpp
{
   string server;
   string login;
   string passwd;
   string resource;
   string alfred;
};

struct Conf
{
   Conf_Xmpp xmpp;
   array<string> modules;
   array<string> friends;
};
